{
  "name": "@fixmyslop/crawl",
  "version": "0.1.1",
  "description": "Deterministic, LLM-free role-aware font crawler (headless Chromium).",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "crawl": "tsx src/crawl.ts"
  },
  "dependencies": {
    "@fixmyslop/core": "*",
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
