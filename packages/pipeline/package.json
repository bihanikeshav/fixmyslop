{
  "name": "@ai-slop-font/pipeline",
  "version": "0.1.0",
  "description": "Ingestion: builds the font index (Google Fonts metadata + glyph metrics + attributes) and saturation signals.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "index": "tsx src/build-index.ts",
    "metrics": "tsx src/extract-metrics.ts",
    "synthetic": "tsx src/sample-synthetic.ts"
  },
  "dependencies": {
    "@ai-slop-font/core": "*",
    "opentype.js": "^1.3.4"
  },
  "devDependencies": {
    "@types/opentype.js": "^1.3.8",
    "tsx": "^4.19.0"
  }
}
