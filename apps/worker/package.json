{
  "name": "fixmyslop-worker",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker wrapping the fixmyslop engine as a REST API and a remote MCP server.",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.121.0"
  }
}
